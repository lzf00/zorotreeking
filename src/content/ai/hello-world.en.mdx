---
lang: en
translationKey: hello-world
title: "Hello World: MDX + Astro Starter"
description: First post to verify MDX rendering, code blocks, inline components, and language switching.
date: 2026-05-11
tags: [meta, astro, mdx]
category: thoughts
---

## Why Astro

> Static-first, islands architecture, zero default JS — for a content-driven personal site, this combo has the lowest writing friction.

Code block test:

```ts
function fib(n: number): number {
  if (n < 2) return n;
  return fib(n - 1) + fib(n - 2);
}
```

Inline `code` works too. **bold**, *italic*, [links](https://astro.build) all render naturally.

## Next steps

1. Wire up Giscus comments
2. Add RSS + sitemap
3. Write the first real AI note
