name = "decap-oauth"
main = "src/index.js"
compatibility_date = "2026-01-01"

# 环境变量在 dashboard 配置：
#   GITHUB_CLIENT_ID      （可见）
#   GITHUB_CLIENT_SECRET  （Secret，加密）
#   ALLOWED_USERS         （可见，例如 "lzf00"）
